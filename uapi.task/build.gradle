dependencies {
    compile (
        project(':uapi.kernel'),
        project(':uapi.kernel.service')
    )
    testCompile (
        project(':uapi.test')
    )
}

jar {
    manifest.attributes
}
