plugins {
    id 'com.github.kt3k.coveralls' version '2.6.3'
}

dependencies {
    compile (
        project(':uapi.kernel')
    )
    testCompile (
        project(':uapi.test')
    )
}

jar {
    manifest.attributes
}
