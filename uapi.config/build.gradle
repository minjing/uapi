dependencies {
    compile project(':uapi.kernel')

    compile "com.fasterxml.jackson.jr:jackson-jr-objects:${jackson_version}"
    compile "com.esotericsoftware.yamlbeans:yamlbeans:${yamlbeans_version}"
}

jar {
    manifest.attributes
}
